import React from 'react'

const Badge = ({ title }) => {
    return (
        <span className='py-2 px-3 text-xs text-[#fff700b0] bg-EveningBlack rounded-full'>{title}</span>
    )
}

export default Badge